# syntax=docker/dockerfile:1
FROM caddy AS caddy

COPY --from=frontend-builder /app/build /usr/share/caddy
COPY Caddyfile /etc/caddy/Caddyfile

CMD ["caddy", "run", "--config", "/etc/caddy/Caddyfile", "--adapter", "caddyfile"]